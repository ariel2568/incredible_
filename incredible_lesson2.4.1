#exercice 1 

def get_words_from_file(filename):
    with open(filename, "r") as file:
        words = file.readlines()
    return [word.strip() for word in words]


import random

def get_random_sentence(length):
    words = get_words_from_file("words.txt") 
    sentence = " ".join(random.sample(words, length))  
    return sentence.lower() 


def main():
    print("welcome , and let s play a game !")
    
    user_input = input("words in sentence ? (entre 2 et 20) : ")

    if user_input.isdigit(): 
        length = int(user_input)
        if 2 <= length <= 20:
            print(get_random_sentence(length))
        else:
            print("Error : must be between 2 and 20 ")
    else:
        print("Error : put a correct number ")




#exercice 2 
import json

sampleJson = """{ 
   "company":{ 
      "employee":{ 
         "name":"emma",
         "payable":{ 
            "salary":7000,
            "bonus":800
         }
      }
   }
}"""

data = json.loads(sampleJson)
salary = data["company"]["employee"]["payable"]["salary"]  
print("Salaire :", salary)

data["company"]["employee"]["birth_date"] = "1990-05-15" 


with open("data.json", "w") as file:
    json.dump(data, file, indent=4) 
