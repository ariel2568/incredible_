#challenge 1 
word = input("give me a word : ")
our_dictionnary = {}
for index, lettre in enumerate(word)
 if lettre in our_dictionnary
 our_dictionnary[lettre].append(index)
 else: 
 our_dictionnary[lettre] = [index]

 #challenge 2 
 items_purchase = {
  "Water": "$1",
  "Bread": "$3",
  "TV": "$1,000",
  "Fertilizer": "$20"
}

wallet = "$300"

wallet = int(wallet.replace("$", "").replace(",", ""))

affordable_items = []

for item, price in items_purchase.items():
    price = int(price.replace("$", "").replace(",", ""))
    if price <= wallet:
        affordable_items.append(item)

affordable_items.sort()

if affordable_items:
    print(affordable_items)
else:
    print("Nothing")
